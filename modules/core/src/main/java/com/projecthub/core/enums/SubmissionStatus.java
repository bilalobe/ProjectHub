package com.projecthub.core.enums;

/**
 * Represents the various statuses a submission can have within the system.
 */
public enum SubmissionStatus {
    /** The submission is pending and awaiting action */
    PENDING,
    /** The submission has been submitted by the user */
    SUBMITTED,
    /** The submission has been graded */
    GRADED
}
