dependencies {
    implementation "org.springframework.modulith:spring-modulith-starter:${versions.springModulith}"
    testImplementation "org.springframework.modulith:spring-modulith-test:${versions.springModulith}"
    
    // Optional - Documentation support
    testImplementation "org.springframework.modulith:spring-modulith-docs:${versions.springModulith}"
    runtimeOnly "org.springframework.modulith:spring-modulith-runtime:${versions.springModulith}"
}