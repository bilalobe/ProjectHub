pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }

    includeBuild 'build-logic'
}

rootProject.name = 'ProjectHub'
include 'core', 'desktop-ui', 'plugins:projecthub-csv-plugin'