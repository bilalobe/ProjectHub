package com.projecthub.core.exceptions;

import lombok.Data;

/**
 * Abstract base class for API sub-errors.
 * Used to provide detailed error information in API responses.
 */
@Data
public abstract class ApiSubError {
    // Base class for sub-error hierarchy
}